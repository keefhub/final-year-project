import { StyleSheet } from "react-native";

const blogStyles = StyleSheet.create({
  container: {
    marginTop: 20,
  },

  modalTitle: {
    marginBottom: 10,
    paddingTop: 10,
  },

  card: {
    padding: 16,
    margin: 10,
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 8,
  },

  title: {
    fontSize: 18,
    fontWeight: "bold",
    marginBottom: 8,
  },

  preview: {
    fontSize: 16,
  },
});
export default blogStyles;
